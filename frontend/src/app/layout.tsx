/**
 * Root layout component
 *
 * This component defines the base layout structure for all pages in the application,
 * including global styles, metadata, and common UI elements like header and footer.
 */

import '@/styles/globals.css'
import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import Header from '@/components/layout/Header'
import Footer from '@/components/layout/Footer'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Secure IaC - Build Secure Cloud Infrastructure',
  description: 'Generate security-focused infrastructure as code templates for your AWS resources',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="flex flex-col min-h-screen">
          <Header />
          <main className="flex-grow">
            {children}
          </main>
          <Footer />
        </div>
      </body>
    </html>
  )
}